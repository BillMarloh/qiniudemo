# 混元3D 2.0 依赖包
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
transformers>=4.48.0
diffusers>=0.21.0
accelerate>=0.20.0
# xformers>=0.0.20  # 可选，可能在某些系统上有问题
# triton>=2.0.0     # 可选，Windows上可能不可用
opencv-python>=4.8.0
Pillow>=9.5.0
numpy>=1.24.0
scipy>=1.10.0
scikit-image>=0.20.0
trimesh>=3.20.0
# open3d>=0.17.0    # 可选，Python 3.13兼容性问题

# 混元3D专用依赖
ninja
pybind11
einops
omegaconf
pymeshlab
pygltflib
xatlas
rembg
onnxruntime

# Web服务依赖
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
pydantic>=2.0.0
aiofiles>=23.0.0
gradio

# 混元3D包（本地安装）
# 将通过 pip install -e ./hunyuan3d 安装

